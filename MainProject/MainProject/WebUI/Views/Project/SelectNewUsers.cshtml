@model List<Core.User>

<h2>Select Users</h2>

@using (Html.BeginForm("AddNewUsersToProject", "Project", FormMethod.Post))
{
    @Html.Hidden("projectId", (Guid)ViewBag.ProjectId) 
    <div class="form-group">
        <table class="table">
            <thead>
                <tr>
                    <th>Username</th>
                    <th>Email</th>
                    <th>Select</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var user in Model)
                {
                    <tr>
                        <td>@user.Username</td>
                        <td>@user.Email</td>
                        <td>
                            <input type="checkbox" name="selectedUsers" value="@user.Id" />
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <button type="submit" class="btn btn-primary button-with-margin">Add Selected Users</button>
}